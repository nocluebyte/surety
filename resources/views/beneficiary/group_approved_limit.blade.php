<table class="table table-separate table-head-custom table-checkable" id="dataTableBuilder">
    <thead>
        <tr>
            <th>{{ __('principle.cin_number') }}</th>
            <th>{{ __('principle.contractor_name') }}</th>
            <th>{{ __('principle.type') }}</th>
            <th>{{ __('principle.country') }} </th>
            <th class="text-right">{{ __('principle.pending_application_limit') }}</th>
            <th class="text-right">{{ __('principle.total_approved_limit') }}</th>
            <th class="text-right">{{ __('principle.converted_inr') }}</th>
            <th class="text-right">{{ __('principle.individual_cap') }}</th>
            <th class="text-right">{{ __('principle.overall_cap') }}</th>
            <th class="text-center">{{ __('principle.regular_review_date') }}</th>
            <th class="text-center">{{ __('principle.from') }}</th>
            <th class="text-center">{{ __('principle.till') }}</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td class="text-center" colspan="12">{{ __('common.no_records_found') }}</td>
        </tr>
    </tbody>
</table>